---
name: sync-main
description: "Safely sync the current branch with main or the default branch. Checks worktree state, fetches, rebases or merges as requested, and handles conflicts carefully."
---

# Sync Main

Read `references/workflow.md` for source behavior. `references/legacy/` is archival only.

Do not discard local changes. Confirm the intended integration strategy when both rebase and merge are viable. Stop and report when conflicts require product judgment.
