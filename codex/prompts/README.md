# Prompt Examples

Codex does not currently load repo-local prompt files as a first-class asset in the way Copilot loads `.prompt.md` files.

Use these files as documented examples and as source material for plugin `interface.defaultPrompt` values or skill-local `prompts/` templates.
