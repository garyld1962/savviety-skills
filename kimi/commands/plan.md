---
name: plan
description: Turn a PRD or requirements file into a validated implementation plan.
---

If `$ARGUMENTS` is a path to a PRD/RFC/spec file, run `/skill:execute-prd $ARGUMENTS`. If no argument is given, ask the user for the requirements source first.
